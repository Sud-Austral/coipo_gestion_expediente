import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { useExpedientes } from '../context/ExpedienteContext.jsx';
import { PERSONAS } from '../fixtures/expedientes.js';
export default function Nueva(){const nav=useNavigate();const{crear}=useExpedientes();const blank={solicitante:'',medio:'Correo',encargo:'',prioridad:'Normal',plazo:'2026-08-14',responsable:'',comentarioInicial:''};const[d,setD]=useState(blank);const set=(k,v)=>setD(x=>({...x,[k]:v}));const submit=(multi)=>{if(!d.solicitante||!d.encargo||!d.plazo)return;crear({...d});if(multi)setD(blank);else nav('/bandeja')};return <div className="mx-auto max-w-4xl space-y-5"><div><div className="text-sm font-semibold text-verde-600">Jefatura</div><h1 className="text-2xl font-bold">➕ Nueva solicitud</h1><p className="mt-1 text-sm text-slate-500">El sistema generará automáticamente el identificador EXP-XXXXXX.</p></div><form className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm" onSubmit={e=>e.preventDefault()}><div className="grid gap-5 md:grid-cols-2"><label className="text-sm font-medium">Solicitante<input value={d.solicitante} onChange={e=>set('solicitante',e.target.value)} className="mt-1.5 w-full rounded-lg border border-slate-200 px-3 py-2.5" placeholder="Persona o área solicitante"/></label><label className="text-sm font-medium">Medio de solicitud<select value={d.medio} onChange={e=>set('medio',e.target.value)} className="mt-1.5 w-full rounded-lg border border-slate-200 px-3 py-2.5"><option>Correo</option><option>Verbal</option><option>Otro</option></select></label><label className="text-sm font-medium md:col-span-2">Encargo<textarea rows="5" value={d.encargo} onChange={e=>set('encargo',e.target.value)} className="mt-1.5 w-full resize-y rounded-lg border border-slate-200 px-3 py-2.5" placeholder="Describa brevemente qué expediente se necesita y qué se requiere realizar."/></label><label className="text-sm font-medium">Prioridad<select value={d.prioridad} onChange={e=>set('prioridad',e.target.value)} className="mt-1.5 w-full rounded-lg border border-slate-200 px-3 py-2.5"><option>Normal</option><option>Media</option><option>Urgente</option></select></label><label className="text-sm font-medium">Fecha comprometida<input type="date" value={d.plazo} onChange={e=>set('plazo',e.target.value)} className="mt-1.5 w-full rounded-lg border border-slate-200 px-3 py-2.5"/></label><label className="text-sm font-medium md:col-span-2">Responsable <span className="font-normal text-slate-400">(opcional)</span><select value={d.responsable} onChange={e=>set('responsable',e.target.value)} className="mt-1.5 w-full rounded-lg border border-slate-200 px-3 py-2.5"><option value="">Sin asignar — quedará en Bandeja</option>{PERSONAS.map(p=><option key={p}>{p}</option>)}</select><span className="mt-1 block text-xs text-slate-500">Si ya sabes quién lo realizará, puedes asignarlo aquí y evitar un paso posterior.</span></label><label className="text-sm font-medium md:col-span-2">Comentario inicial <span className="font-normal text-slate-400">(opcional)</span><textarea rows="3" value={d.comentarioInicial} onChange={e=>set('comentarioInicial',e.target.value)} className="mt-1.5 w-full rounded-lg border border-slate-200 px-3 py-2.5" placeholder="Información adicional para el inicio del expediente..."/></label></div><div className="mt-6 flex flex-wrap justify-end gap-3 border-t border-slate-100 pt-5"><button type="button" onClick={()=>nav('/')} className="rounded-lg border border-slate-200 px-4 py-2.5 text-sm font-semibold">Cancelar</button><button type="button" onClick={()=>submit(false)} className="rounded-lg bg-verde-600 px-4 py-2.5 text-sm font-semibold text-white">Crear solicitud</button><button type="button" onClick={()=>submit(true)} className="rounded-lg border border-verde-600 px-4 py-2.5 text-sm font-semibold text-verde-700">Crear varias solicitudes</button></div></form></div>}
